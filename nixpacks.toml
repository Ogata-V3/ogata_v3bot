[phases.setup]
nixPkgs = [
  "nodejs_18",
  "cairo",
  "pango",
  "libpng",
  "libjpeg",
  "giflib",
  "librsvg",
  "pkgconfig",
  "python3",
  "gcc",
  "gnumake"
]

[phases.install]
cmds = ["npm install --force"]

[start]
cmd = "node index.js"
